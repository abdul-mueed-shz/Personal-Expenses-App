import 'package:flutter/material.dart';

class Transx {
  String ID;
  String title;
  double amount;
  DateTime date;

  Transx(
      {required this.ID,
      required this.title,
      required this.amount,
      required this.date});
}
